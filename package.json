{
  "name": "bg-organic-registry",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "description": "Fast, searchable mirror of the Bulgarian organic-production operators registry",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-data": "python scripts/fetch_data.py"
  },
  "dependencies": {
    "tabulator-tables": "^6.3.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
